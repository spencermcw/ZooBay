name: zoobay-nginx
region: nyc
services:
- dockerfile_path: nginx/Dockerfile
  github:
    branch: master
    repo: spencermcw/ZooBay
  http_port: 8080
  instance_count: 1
  instance_size_slug: basic-xxs
  name: zoobay-nginx
  routes:
  - path: /
  source_dir: nginx
